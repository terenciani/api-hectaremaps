"use strict";

module.exports = class EmailService {

    static async send() {
        try {
            return "TODO"
        } catch (error) {
            throw new Error("ServiceService.send: " + error);
        }
    } // send()
} // class